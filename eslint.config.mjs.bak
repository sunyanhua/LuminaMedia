import eslint from '@eslint/js';

export default [
  {
    ignores: ['dist', 'node_modules', 'coverage', '*.js'],
  },
  eslint.configs.recommended,
  {
    rules: {
      'no-unused-vars': 'warn',
    },
  },
];